import styled from 'styled-components';
import theme from "../../constants/theme";
import {BigTitle} from "../Text";

export const Container = styled.View`
    
    
`;

export const BarTitle = styled(BigTitle)`
    font-size: ${theme.fontSize.normal}px;
`
